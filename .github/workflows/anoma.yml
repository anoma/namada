name: Anoma CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - master

jobs:
  build:
    if: github.event.pull_request.draft == false
    name: Build Anoma
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/work/anoma-prototype/anoma-prototype/matchmaker_template/target
            ~/work/anoma-prototype/anoma-prototype/tx_template/target
            ~/work/anoma-prototype/anoma-prototype/vp_template/target
            ~/work/anoma-prototype/anoma-prototype/target
          key: cache-v4-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cache-v4-cargo-
      - name: Run requirements
        run: |
              make -C tx_template deps
              make build-wasm-scripts
      - name: Run build
        run: make build
      - name: Run test
        run: make test

  format:
    if: github.event.pull_request.draft == false
    name: Format and Lint Anoma
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          # This should match the nightly version specified in the root Makefile
          toolchain: nightly-2021-03-09
          components: rustfmt, clippy
          override: true
      - uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/work/anoma-prototype/anoma-prototype/matchmaker_template/target
            ~/work/anoma-prototype/anoma-prototype/tx_template/target
            ~/work/anoma-prototype/anoma-prototype/vp_template/target
            ~/work/anoma-prototype/anoma-prototype/target
          key: cache-v4-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cache-v4-cargo- 
      - name: Run fmt
        run: make fmt-check
      - name: Run clippy
        run: make clippy-check