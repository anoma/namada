---
kind: pipeline
type: docker
name: anoma-ci-build-pr

workspace:
  path: /drone/workspace

environment:
  CARGO_HOME: /drone/workspace/.cargo
  RUSTUP_HOME: /drone/workspace/.cargo/bin
  # CARGO_TARGET_DIR: /drone/workspace/target
  GIT_LFS_SKIP_SMUDGE: "1"
  CARGO_INCREMENTAL: 0

steps:
  - name: install-rust
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
      - . /drone/workspace/.cargo/env
      - rustup toolchain install stable

  - name: restore-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      restore: true
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      cache_key: "{{ checksum \"Cargo.lock\" }}"
      archive_format: "gzip"
      mount:
        - .cargo/bin/
        - .cargo/registry/index/
        - .cargo/registry/cache/
        - .cargo/git/db/
        - matchmaker_template/target
        - txs/tx_from_intent/target
        - txs/tx_template/target
        - txs/tx_transfer/target
        - vps/vp_template/target
        - vps/vp_token/target
        - vps/vp_user/target
        - txs/tx_update_vp
        - filter_template
        - target
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
    depends_on:
      - install-rust

  - name: build
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - . /drone/workspace/.cargo/env
      - which rustup
      - which rustc
      - which cargo
      - make -C txs/tx_template deps
      - make build-wasm-scripts
      - make build
    depends_on:
      - restore-cache

  - name: test
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - . /drone/workspace/.cargo/env
      - which rustup
      - which rustc
      - which cargo
      - make test
    depends_on:
      - build

  - name: rebuild-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      rebuild: true
      archive_format: "gzip"
      override: false
      cache_key: "{{ checksum \"Cargo.lock\" }}"
      mount:
        - .cargo/bin/
        - .cargo/registry/index/
        - .cargo/registry/cache/
        - .cargo/git/db/
        - matchmaker_template/target
        - txs/tx_from_intent/target
        - txs/tx_template/target
        - txs/tx_transfer/target
        - vps/vp_template/target
        - vps/vp_token/target
        - vps/vp_user/target
        - txs/tx_update_vp
        - filter_template
        - target
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
      BACKEND_OPERATION_TIMEOUT: "8m"
    when:
      status:
        - success
        - failure
    depends_on:
      - test

trigger:
  event:
    - pull_request

node:
  project: anoma

---
kind: pipeline
type: docker
name: anoma-ci-checks-pr

workspace:
  path: /drone/workspace

environment:
  CARGO_HOME: /drone/workspace/.cargo
  RUSTUP_HOME: /drone/workspace/.cargo/bin
  # CARGO_TARGET_DIR: /drone/workspace/target
  GIT_LFS_SKIP_SMUDGE: "1"
  CARGO_INCREMENTAL: 0

steps:
  - name: install-rust
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

  - name: restore-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      restore: true
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      cache_key: "nightly/{{ checksum \"Cargo.lock\" }}"
      archive_format: "gzip"
      mount:
        - .cargo/bin/
        - .cargo/registry/index/
        - .cargo/registry/cache/
        - .cargo/git/db/
        - target
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
    depends_on:
      - install-rust

  - name: clippy-fmt-checks
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - . /drone/workspace/.cargo/env
      - which rustup
      - which rustc
      - which cargo
      - make dev-deps
      - make clippy-check
      - make fmt-check
    depends_on:
      - restore-cache

  - name: rebuild-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      rebuild: true
      archive_format: "gzip"
      override: false
      cache_key: "nightly/{{ checksum \"Cargo.lock\" }}"
      mount:
        - .cargo/bin/
        - .cargo/registry/index/
        - .cargo/registry/cache/
        - .cargo/git/db/
        - target
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
      BACKEND_OPERATION_TIMEOUT: "8m"
    when:
      status:
        - success
        - failure
    depends_on:
      - clippy-fmt-checks
  
trigger:
  event:
    - pull_request

node:
  project: anoma

---
kind: pipeline
type: docker
name: anoma-ci-audit-pr

workspace:
  path: /drone/workspace

environment:
  CARGO_HOME: /drone/workspace/.cargo
  RUSTUP_HOME: /drone/workspace/.cargo/bin
  GIT_LFS_SKIP_SMUDGE: "1"
  CARGO_INCREMENTAL: 0

steps:
  - name: install-rust
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

  - name: restore-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      restore: true
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      cache_key: "audit/{{ checksum \"Cargo.lock\" }}"
      archive_format: "gzip"
      mount:
        - .cargo/bin/
        - .cargo/registry/index/
        # - .cargo/registry/cache/
        - .cargo/git/db/
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
    depends_on:
      - install-rust

  - name: audit
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - . /drone/workspace/.cargo/env
      - cargo install cargo-audit
      - make audit
    depends_on:
      - install-rust

  - name: rebuild-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      rebuild: true
      archive_format: "gzip"
      override: false
      cache_key: "audit/{{ checksum \"Cargo.lock\" }}"
      mount:
        - .cargo/bin/
        - .cargo/registry/index/
        # - .cargo/registry/cache/
        - .cargo/git/db/
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
      BACKEND_OPERATION_TIMEOUT: "8m"
    when:
      status:
        - success
        - failure
    depends_on:
      - restore-cache
  
trigger:
  event:
    - pull_request

node:
  project: anoma

---
kind: pipeline
type: docker
name: anoma-ci-build-master-develop

workspace:
  path: /drone/workspace

environment:
  CARGO_HOME: /drone/workspace/.cargo
  RUSTUP_HOME: /drone/workspace/.cargo/bin
  GIT_LFS_SKIP_SMUDGE: "1"
  CARGO_INCREMENTAL: 0

steps:
  - name: restore-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      restore: true
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      cache_key: "{{ checksum \"Cargo.lock\" }}"
      archive_format: "gzip"
      mount:
        - .cargo/git
        - .cargo/registry
        - matchmaker_template/target
        - txs/tx_from_intent/target
        - txs/tx_template/target
        - txs/tx_transfer/target
        - vps/vp_template/target
        - vps/vp_token/target
        - vps/vp_user/target
        - txs/tx_update_vp
        - filter_template
        - target
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key

  - name: install-rust
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
    depends_on:
      - restore-cache

  - name: build
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - . /drone/workspace/.cargo/env
      - make -C txs/tx_template deps
      - make build-wasm-scripts
      - make build
    depends_on:
      - install-rust

  - name: test
    image: 965844283396.dkr.ecr.eu-west-1.amazonaws.com/anoma:latest
    pull: if-not-exists
    commands:
      - . /drone/workspace/.cargo/env
      - make test
    depends_on:
      - build

  - name: rebuild-cache
    image: meltwater/drone-cache
    pull: if-not-exists
    settings:
      backend: "s3"
      bucket: heliax-drone-cache-v2
      region: eu-west-1
      rebuild: true
      archive_format: "gzip"
      override: false
      cache_key: "{{ checksum \"Cargo.lock\" }}"
      mount:
        - .cargo/git
        - .cargo/registry
        - matchmaker_template/target
        - txs/tx_from_intent/target
        - txs/tx_template/target
        - txs/tx_transfer/target
        - vps/vp_template/target
        - vps/vp_token/target
        - vps/vp_user/target
        - txs/tx_update_vp
        - filter_template
        - target
    environment:
      AWS_ACCESS_KEY_ID:
        from_secret: aws_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: aws_secret_access_key
      BACKEND_OPERATION_TIMEOUT: "8m"
    when:
      status:
        - success
        - failure
    depends_on:
      - test
  
trigger:
  event:
    - push
  branch:
    - develop
    - master

node:
  project: anoma
---
kind: signature
hmac: bd26512545713cedd44fd5e37a5fb65fcb2ba88afc56bfb683e4d89c0bae9e21

...
