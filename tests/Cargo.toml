[package]
authors = ["Heliax AG <hello@heliax.dev>"]
description = "Anoma tests"
edition = "2018"
license = "GPL-3.0"
name = "anoma_tests"
version = "0.1.0"

[dependencies]
anoma = {path = "../apps", features = ["testing"]}
anoma_shared = {path = "../shared", features = ["testing"]}
anoma_vm_env = {path = "../vm_env"}
concat-idents = "1.1.2"
